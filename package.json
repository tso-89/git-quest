{
  "name": "git-quest",
  "version": "1.0.0",
  "private": true,
  "description": "An interactive git lesson for beginners working alongside AI coding agents.",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify": "npm run lint && npm run build && node --test \"tests/**/*.test.mjs\"",
    "test": "npm run build && node --test \"tests/**/*.test.mjs\"",
    "test:unit": "node --test \"tests/unit/*.test.mjs\"",
    "test:e2e": "npm run build && node --test \"tests/e2e/*.test.mjs\"",
    "lint": "for f in src/js/*.js scripts/*.mjs; do node --check \"$f\" || exit 1; done",
    "dev": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
